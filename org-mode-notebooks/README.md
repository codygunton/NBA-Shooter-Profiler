# Org-mode Jupyter notebooks.
x
These are notebooks for an Emacs org-mode + Jupyter IDE I use when running code locally. I used exported these to markdown to produce the files in the top-level directory.
